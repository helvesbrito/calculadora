name: Build

on:
  push:
    branches:
      - master
      - develop
      - 'feature/**'
      - 'bugfix/**'

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3

      - name: compile-project
        shell: powershell
        run: .\compilador.bat

      - name: execute-test
        shell: powershell
        run: C:\Users\helve\Documents\Embarcadero\Studio\Projects\calculadora\Test\Win32\Debug\PrjCalculadoraTests.exe